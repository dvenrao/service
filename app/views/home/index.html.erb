<% @posts.each do |post| %>
<div class="row">
  <div class="col-sm-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title"><%= post.title %></h3>
      </div>
      <div class="panel-body">
        <div class="row">
          <div class="col-sm-2">
            <img alt="120x120" class="img-thumbnail" data-src="holder.js/200x200" style="width: 120px; height: 120px;" src="">
          </div>
          <div class="col-sm-8">
            <ul class="list-unstyled">
              <li><%= post.category.name if post.category %></li>
              <li><%= post.locality.name if post.locality %> - <%= post.city %> </li>
            </ul>

            <p><%= post.description %></p>
            <!--<p><%#= post.created_at %></p>-->
          </div>
          <div class="col-sm-2">
            <p><a class="btn btn-default" href="#" role="button">View details &raquo;</a></p>
            <p>Ph: <%= post.mobile_no %></p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<% end %>